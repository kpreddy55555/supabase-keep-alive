name: Keep Supabase Alive

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 */5 * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Project
        run: >
          curl -I "https://ftbbqjmranyindlyahc.supabase.co" 
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}"
          -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
